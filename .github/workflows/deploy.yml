name: Deploy Telegram Bot

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Setup WARP
        uses: fscarmen/warp-on-actions@v1.0
        with:
          mode: client

      - name: 🛠️ Настраиваем WARP для работы с IPv6
        run: |
          warp-cli registration
          warp-cli set-mode proxy
          warp-cli connect
          echo "Текущий IPv6-адрес:"
          curl -6 ifconfig.co || echo "IPv6 недоступен"

      - name: 📡 Проверяем подключение по IPv6
        run: |
          ping6 -c 4 ${{ secrets.SERVER_IPV6 }} || echo "IPv6 недоступен"

      - name: 🔄 Клонируем репозиторий
        uses: actions/checkout@v4

      - name: 🔑 Устанавливаем SSH-ключ
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/github-actions-key
          chmod 600 ~/.ssh/github-actions-key
          ssh-keyscan -H [${{ secrets.SERVER_IPV6 }}] >> ~/.ssh/known_hosts

      - name: 🚀 Деплоим на сервер
        run: |
          ssh -i ~/.ssh/github-actions-key ${{ secrets.SERVER_USER }}@[${{ secrets.SERVER_IPV6 }}] << 'EOF'
            cd /root/my-bot/
            git pull origin main
            source venv/bin/activate
            pip install -r requirements.txt
            systemctl restart my-bot
          EOF
